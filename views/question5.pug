extends layout

block content
  link(rel='stylesheet' href='./css/default.css')
  link(rel='stylesheet' href='./css/main.css')
  section(id='main' class ='mx-auto my-5 py-5 px-3')
    h1(class = ' col-lg-6 col-md-8 col-sm-10 col-12 mx-auto') 시험을 망쳤을 때 나는?
  
    a(href=`http://localhost:3000/loding?point=${point+1}`)
      button(class='btn btn-primary btn-lg btn-block' type='button') 에라 모르겠다 하고 논다
    a(href=`http://localhost:3000/loding?point=${point+2}`)
      button(class='btn btn-secondary btn-lg btn-block' type='button') 울며 다음 시험을 계획한다